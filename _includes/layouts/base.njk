<!doctype html>
<html lang="en">
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>{{ title }}{% if title %} · {% endif %}{{ metadata.title }}</title>

	<link rel="stylesheet" href="{% getBundleFileUrl "css" %}">

	<link rel="preconnect" href="https://fonts.googleapis.com">
	<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
	<link href="https://fonts.googleapis.com/css2?family=Source+Code+Pro:wght@300;400;600;700&display=swap" rel="stylesheet">
</head>
<body>

	{% if page.url != "/" %}
		<div class="topbar">
			<a class="topbar-home" href="/">Return to Home</a>
		</div>
	{% endif %}

	<main id="main">
		{{ content | safe }}
	</main>

	<script type="module" src="{% getBundleFileUrl "js" %}"></script>
</body>
</html>
